// link to manager class

describe("Manager", () => {
  // officeNumber
  // // it should have a officenumber input
  it("should have a officenumber input", () => {
    // // officeNum = officeNum
  });

  describe("getRole", () => {
    // getRole()
    // // it should overridden to return Manager
    it("should overridden to return Manager", () => {
      // // expect role = role
    });
  });
});
